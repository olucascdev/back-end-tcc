diff --git a/.codenomad/worktreeMap.json b/.codenomad/worktreeMap.json
index 6b0ade2..b622f6b 100644
--- a/.codenomad/worktreeMap.json
+++ b/.codenomad/worktreeMap.json
@@ -8,6 +8,7 @@
     "ses_228eb40a4ffeKMdg032SXGXOi3": "root",
     "ses_226e3a223ffe4HwFkopCMBYAEm": "root",
     "ses_2253d20a3ffenoVFHtCJRJ6eB0": "root",
-    "ses_22534e8d0ffevvvghKPfwU6BTe": "root"
+    "ses_22534e8d0ffevvvghKPfwU6BTe": "root",
+    "ses_1c9d28bd3ffewVSKbA2g1LWt8G": "root"
   }
 }
\ No newline at end of file
diff --git a/services/python-agent/.env.example b/services/python-agent/.env.example
index 2ea5f4b..3d754d1 100644
--- a/services/python-agent/.env.example
+++ b/services/python-agent/.env.example
@@ -9,6 +9,9 @@ PYTHON_AGENT_HOST=0.0.0.0
 DB_URL=postgres://tcc_user:tcc_pass@postgres:5432/tcc_db
 
 # OpenAI (or compatible LLM provider)
+# For default OpenAI: leave OPENAI_BASE_URL empty.
+# For Groq: set OPENAI_BASE_URL=https://api.groq.com/openai/v1 and use a Groq model.
+# For other OpenAI-compatible providers: set the appropriate base_url.
 OPENAI_API_KEY=sk-placeholder-replace-me
 OPENAI_MODEL=gpt-4o-mini
 OPENAI_BASE_URL=
diff --git a/services/python-agent/README.md b/services/python-agent/README.md
index 6e62516..18102ed 100644
--- a/services/python-agent/README.md
+++ b/services/python-agent/README.md
@@ -52,5 +52,21 @@ See `.env.example` for all available options. Key settings:
 - `PYTHON_AGENT_PORT` - HTTP server port (default: 8000)
 - `DB_URL` - PostgreSQL connection with pgvector
 - `OPENAI_API_KEY` - LLM provider API key
+- `OPENAI_MODEL` - Model name (e.g. `gpt-4o-mini`, `llama-3.3-70b-versatile`)
+- `OPENAI_BASE_URL` - Base URL for OpenAI-compatible providers (empty = default OpenAI)
 - `MINIO_*` - Object storage for document artifacts
 - `CHUNK_SIZE` / `CHUNK_OVERLAP` - Text splitting parameters
+
+### Using Groq (OpenAI-compatible)
+
+Groq provides an OpenAI-compatible API with lower latency and cost. To use it:
+
+```env
+OPENAI_API_KEY=gsk_your_groq_key_here
+OPENAI_MODEL=llama-3.3-70b-versatile
+OPENAI_BASE_URL=https://api.groq.com/openai/v1
+```
+
+Supported Groq models include `llama-3.3-70b-versatile`, `llama-3.1-8b-instant`, `mixtral-8x7b-32768`, and others. Check [Groq's model list](https://console.groq.com/docs/models) for available options.
+
+**Note:** Embeddings still use the OpenAI API by default (`EMBEDDING_MODEL=text-embedding-3-small`). If your provider does not support embeddings, the service will fall back to mock embeddings for development.
diff --git a/services/python-agent/app/core/config.py b/services/python-agent/app/core/config.py
index 2cb4dbc..57e71b7 100644
--- a/services/python-agent/app/core/config.py
+++ b/services/python-agent/app/core/config.py
@@ -28,6 +28,7 @@ class Settings(BaseSettings):
     ENABLE_PUBLIC_RETRIEVAL: bool = False
     # RAG / LLM settings
     OPENAI_MODEL: str = "gpt-4o-mini"
+    OPENAI_BASE_URL: str = ""  # Leave empty for default OpenAI; set for Groq or other compatible providers
     MAX_CONTEXT_TOKENS: int = 4000
     SYSTEM_PROMPT: str = (
         "Voce e um assistente especializado em analisar documentos academicos. "
diff --git a/services/python-agent/app/domain/compare_service.py b/services/python-agent/app/domain/compare_service.py
index b04edf3..95b8747 100644
--- a/services/python-agent/app/domain/compare_service.py
+++ b/services/python-agent/app/domain/compare_service.py
@@ -260,10 +260,14 @@ class CompareService:
         ]
 
         if not self._embedder._use_mock:
-            # Chamada real a OpenAI
+            # Chamada real a OpenAI (ou provider compativel via base_url)
             from openai import OpenAI
 
-            client = OpenAI(api_key=self._settings.OPENAI_API_KEY)
+            base_url = self._settings.OPENAI_BASE_URL or None
+            client = OpenAI(
+                api_key=self._settings.OPENAI_API_KEY,
+                base_url=base_url,
+            )
             try:
                 response = client.chat.completions.create(
                     model=self._settings.OPENAI_MODEL,
diff --git a/services/python-agent/app/domain/rag_service.py b/services/python-agent/app/domain/rag_service.py
index 55fcc93..20bb9e4 100644
--- a/services/python-agent/app/domain/rag_service.py
+++ b/services/python-agent/app/domain/rag_service.py
@@ -297,10 +297,14 @@ class RAGService:
         messages.append({"role": "user", "content": user_content})
 
         if not self._embedder._use_mock:
-            # Chamada real a OpenAI
+            # Chamada real a OpenAI (ou provider compativel via base_url)
             from openai import OpenAI
 
-            client = OpenAI(api_key=self._settings.OPENAI_API_KEY)
+            base_url = self._settings.OPENAI_BASE_URL or None
+            client = OpenAI(
+                api_key=self._settings.OPENAI_API_KEY,
+                base_url=base_url,
+            )
             try:
                 response = client.chat.completions.create(
                     model=self._settings.OPENAI_MODEL,
diff --git a/services/python-agent/app/domain/research_gap_service.py b/services/python-agent/app/domain/research_gap_service.py
index ed6ef27..6726035 100644
--- a/services/python-agent/app/domain/research_gap_service.py
+++ b/services/python-agent/app/domain/research_gap_service.py
@@ -220,10 +220,14 @@ class ResearchGapService:
         ]
 
         if not self._embedder._use_mock:
-            # Chamada real a OpenAI
+            # Chamada real a OpenAI (ou provider compativel via base_url)
             from openai import OpenAI
 
-            client = OpenAI(api_key=self._settings.OPENAI_API_KEY)
+            base_url = self._settings.OPENAI_BASE_URL or None
+            client = OpenAI(
+                api_key=self._settings.OPENAI_API_KEY,
+                base_url=base_url,
+            )
             try:
                 response = client.chat.completions.create(
                     model=self._settings.OPENAI_MODEL,
diff --git a/services/python-agent/app/domain/summarize_service.py b/services/python-agent/app/domain/summarize_service.py
index 2f3ada5..8fd1947 100644
--- a/services/python-agent/app/domain/summarize_service.py
+++ b/services/python-agent/app/domain/summarize_service.py
@@ -218,10 +218,14 @@ class SummarizeService:
         ]
 
         if not self._embedder._use_mock:
-            # Chamada real a OpenAI
+            # Chamada real a OpenAI (ou provider compativel via base_url)
             from openai import OpenAI
 
-            client = OpenAI(api_key=self._settings.OPENAI_API_KEY)
+            base_url = self._settings.OPENAI_BASE_URL or None
+            client = OpenAI(
+                api_key=self._settings.OPENAI_API_KEY,
+                base_url=base_url,
+            )
             try:
                 response = client.chat.completions.create(
                     model=self._settings.OPENAI_MODEL,
diff --git a/services/python-agent/app/infrastructure/embeddings/openai_embedder.py b/services/python-agent/app/infrastructure/embeddings/openai_embedder.py
index 20e52bb..a4e2284 100644
--- a/services/python-agent/app/infrastructure/embeddings/openai_embedder.py
+++ b/services/python-agent/app/infrastructure/embeddings/openai_embedder.py
@@ -33,7 +33,12 @@ class OpenAIEmbedder:
         else:
             from openai import OpenAI
 
-            self._client = OpenAI(api_key=self._settings.OPENAI_API_KEY)
+            # Usa base_url se configurado (ex: Groq ou outro provider compativel)
+            base_url = self._settings.OPENAI_BASE_URL or None
+            self._client = OpenAI(
+                api_key=self._settings.OPENAI_API_KEY,
+                base_url=base_url,
+            )
             self._model = self._settings.EMBEDDING_MODEL
 
     def embed_query(self, text: str) -> list[float]:
